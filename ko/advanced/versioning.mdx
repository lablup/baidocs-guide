# 버전 관리 및 브랜치 관리

BaiDocs는 Git 브랜치 통합을 통해 강력한 버전 관리 기능을 제공하여 원활한 전환, 버전별 콘텐츠 및 자동화된 버전 관리를 통해 문서의 여러 버전을 유지할 수 있습니다.

## 개요

### 버전 관리 기능

- **Git 브랜치 통합**: 각 문서 버전이 Git 브랜치에 대응
- **자동 버전 감지**: 사용 가능한 버전 자동 감지 및 나열
- **버전 전환**: 다른 문서 버전 간 원활한 전환
- **버전별 콘텐츠**: 버전 간에 다른 콘텐츠
- **레거시 지원**: 새 버전 개발 중 이전 버전 유지
- **릴리스 관리**: 자동화된 버전 태깅 및 릴리스 프로세스
- **버전 비교**: 다른 버전 간 콘텐츠 비교

### 일반적인 버전 관리 시나리오

- **제품 릴리스**: v1.0, v2.0, v3.0 문서 유지
- **API 버전**: 다양한 API 버전 문서화 (v1, v2, latest)
- **기능 브랜치**: 향후 기능에 대한 문서 미리보기
- **환경별**: 스테이징, 프로덕션 환경별 다른 문서
- **현지화된 버전**: 지역 또는 고객 세그먼트별 다른 버전

## 브랜치 기반 버전 관리

### 버전 구성

책 설정에서 버전 관리를 구성할 수 있습니다:

```yaml
# book.config.yaml
id: my-documentation
title: My Documentation
description: Multi-version documentation

# Git 구성
git:
  repository: "https://github.com/myorg/docs.git"
  branch: main  # 기본 브랜치

# 버전 구성
versioning:
  enabled: true
  strategy: "branch"  # 또는 "tag", "directory"

  # 버전 매핑
  versions:
    - branch: "main"
      version: "latest"
      label: "Latest (v3.0)"
      isDefault: true
      status: "active"

    - branch: "v2.x"
      version: "v2"
      label: "Version 2.x"
      status: "active"

    - branch: "v1.x"
      version: "v1"
      label: "Version 1.x"
      status: "maintenance"

    - branch: "develop"
      version: "beta"
      label: "Beta (v3.1)"
      status: "preview"

  # 버전별 설정
  versionConfig:
    latest:
      supportStatus: "active"
      endOfLife: null
      releaseDate: "2024-01-26"

    v2:
      supportStatus: "maintenance"
      endOfLife: "2024-12-31"
      releaseDate: "2023-01-15"

    v1:
      supportStatus: "legacy"
      endOfLife: "2024-06-30"
      releaseDate: "2022-01-10"
```

### Git 브랜치 구조

버전 관리를 위해 Git 브랜치를 구성할 수 있습니다:

```
Git 저장소 구조:

main                    ← 최신 안정 버전 (v3.0)
├── content/
│   ├── book.config.yaml
│   ├── en/
│   └── ko/

v2.x                    ← 버전 2.x 유지보수
├── content/
│   ├── book.config.yaml  (버전별 구성)
│   ├── en/
│   └── ko/

v1.x                    ← 레거시 버전 1.x
├── content/
│   ├── book.config.yaml  (이전 구성)
│   ├── en/
│   └── ko/

develop                 ← 개발 브랜치 (v3.1 베타)
├── content/
│   ├── book.config.yaml
│   ├── en/
│   └── ko/

feature/new-api         ← 기능 미리보기
├── content/
│   ├── book.config.yaml
│   ├── en/
│   └── ko/
```

## 버전 관리

### 새 버전 생성

#### 메인에서 릴리스 버전 생성

새로운 안정 버전을 생성할 수 있습니다:

```bash
# 메인에서 버전 브랜치 생성
git checkout main
git checkout -b v3.x

# 버전 구성 업데이트
# book.config.yaml을 편집하여 새 버전 반영

# 새 버전 브랜치 푸시
git push origin v3.x

# 다음 버전을 위해 메인에서 개발 계속
git checkout main
# 다음 개발 버전을 나타내도록 버전 업데이트
```

#### 기능 브랜치 미리보기

새 기능에 대한 미리보기 버전을 생성할 수 있습니다:

```bash
# 기능 브랜치 생성
git checkout -b feature/new-authentication

# 미리보기용 book.config.yaml 업데이트
git add content/book.config.yaml
git commit -m "Configure preview version for new authentication"
git push origin feature/new-authentication
```

### 버전별 구성

각 버전은 자체 구성을 가질 수 있습니다:

```yaml
# main 브랜치 - book.config.yaml
id: my-documentation
title: "My Documentation v3.0"
version: "3.0"

# 버전별 메타데이터
versionInfo:
  major: 3
  minor: 0
  patch: 0
  prerelease: null
  status: "stable"

# 버전별 네비게이션
navigation:
  en:
    - title: "Home"
      path: "index.mdx"
    - title: "New Features"  # v3.0에만 존재
      path: "new-features.mdx"
    - title: "API Reference"
      items:
        - title: "v3 API"      # 버전별 API 문서
          path: "api/v3/index.mdx"
```

```yaml
# v2.x 브랜치 - book.config.yaml
id: my-documentation
title: "My Documentation v2.x"
version: "2.x"

versionInfo:
  major: 2
  minor: 8
  patch: 3
  prerelease: null
  status: "maintenance"

# v2.x에 대한 다른 네비게이션
navigation:
  en:
    - title: "Home"
      path: "index.mdx"
    - title: "Migration Guide"  # 사용자가 v3로 마이그레이션하도록 도움
      path: "migration-to-v3.mdx"
    - title: "API Reference"
      items:
        - title: "v2 API"      # 레거시 API 문서
          path: "api/v2/index.mdx"
```

## 버전 인식 콘텐츠

### 조건부 콘텐츠

버전에 따라 콘텐츠를 표시할 수 있습니다:

```mdx
import { useVersion } from '@baidocs/versioning'

# API 인증

인증 방법은 버전에 따라 다릅니다:

<VersionContent version="3.x">

## OAuth 2.0 인증 (v3.0+)

버전 3.0은 향상된 보안을 위해 OAuth 2.0을 도입합니다:

```javascript
const auth = new OAuth2Client({
  clientId: 'your-client-id',
  redirectUri: 'https://your-app.com/callback'
});

await auth.authenticate();
```

</VersionContent>

<VersionContent version={["2.x", "1.x"]}>

## API 키 인증 (v1.x - v2.x)

레거시 버전은 API 키 인증을 사용합니다:

```javascript
const client = new APIClient({
  apiKey: 'your-api-key'
});
```

:::warning 더 이상 사용되지 않음
API 키 인증은 v3.0에서 더 이상 사용되지 않습니다. OAuth 2.0으로 마이그레이션하세요.
:::

</VersionContent>

<VersionContent version="1.x">

## 기본 인증 (v1.x만)

버전 1.x는 기본 인증도 지원합니다:

```javascript
const client = new APIClient({
  username: 'your-username',
  password: 'your-password'
});
```

:::danger 보안 알림
기본 인증은 보안상의 이유로 v2.0+에서 제거되었습니다.
:::

</VersionContent>
```

### 버전 비교

버전 간 기능을 비교할 수 있습니다:

```mdx
import { VersionTable, VersionCompare } from '@baidocs/components'

## 기능 비교

<VersionTable>
| 기능 | v1.x | v2.x | v3.x |
|---------|------|------|------|
| 기본 인증 | ✅ | ❌ | ❌ |
| API 키 | ❌ | ✅ | ⚠️ 더 이상 사용되지 않음 |
| OAuth 2.0 | ❌ | ❌ | ✅ |
| 속도 제한 | 기본 | 고급 | 스마트 |
| WebSocket API | ❌ | ✅ | ✅ 향상됨 |
| GraphQL | ❌ | ❌ | ✅ |
</VersionTable>

<VersionCompare versions={['2.x', '3.x']}>

### v2.x에서 v3.x로 마이그레이션

업그레이드 시 주요 변경사항:

#### 주요 변경사항
- API 키 인증 더 이상 사용되지 않음
- WebSocket 프로토콜 업데이트
- 일부 엔드포인트 URL 변경

#### 새 기능
- OAuth 2.0 인증
- GraphQL API
- 향상된 속도 제한

</VersionCompare>
```

### 버전별 어드모니션

버전 인식 콜아웃을 생성할 수 있습니다:

```mdx
import { VersionAdmonition } from '@baidocs/components'

<VersionAdmonition type="added" version="3.0">
**v3.0의 새 기능:** OAuth 2.0 인증으로 향상된 보안과 사용자 경험을 제공합니다.
</VersionAdmonition>

<VersionAdmonition type="changed" version="2.5">
**v2.5에서 변경됨:** WebSocket 연결 처리가 더 나은 안정성을 위해 개선되었습니다.
</VersionAdmonition>

<VersionAdmonition type="deprecated" version="3.0" removeVersion="4.0">
**v3.0에서 더 이상 사용되지 않음:** API 키 인증은 v4.0에서 제거될 예정입니다. OAuth 2.0으로 마이그레이션하세요.
</VersionAdmonition>

<VersionAdmonition type="removed" version="2.0" previousVersion="1.x">
**v2.0에서 제거됨:** 기본 인증이 보안상의 이유로 제거되었습니다.
</VersionAdmonition>
```

## 버전 전환기 인터페이스

### 버전 선택기 컴포넌트

버전 전환 인터페이스를 생성할 수 있습니다:

```typescript
// components/VersionSwitcher.tsx
import React, { useState } from 'react';
import { useRouter } from 'next/router';
import { useVersioning } from '@baidocs/versioning';

interface Version {
  branch: string;
  version: string;
  label: string;
  status: 'active' | 'maintenance' | 'legacy' | 'preview';
  isDefault: boolean;
}

export function VersionSwitcher() {
  const router = useRouter();
  const { currentVersion, availableVersions, switchVersion } = useVersioning();
  const [isOpen, setIsOpen] = useState(false);

  const handleVersionSwitch = async (version: Version) => {
    try {
      await switchVersion(version.version);

      // 버전 변경을 반영하기 위해 URL 업데이트
      const currentPath = router.asPath;
      const newPath = currentPath.replace(
        `/docs/${currentVersion}/`,
        `/docs/${version.version}/`
      );

      router.push(newPath);
      setIsOpen(false);
    } catch (error) {
      console.error('Failed to switch version:', error);
    }
  };

  const getStatusColor = (status: string) => {
    switch (status) {
      case 'active': return '#10b981';
      case 'maintenance': return '#f59e0b';
      case 'legacy': return '#ef4444';
      case 'preview': return '#8b5cf6';
      default: return '#6b7280';
    }
  };

  const getStatusLabel = (status: string) => {
    switch (status) {
      case 'active': return '활성';
      case 'maintenance': return '유지보수';
      case 'legacy': return '레거시';
      case 'preview': return '미리보기';
      default: return '';
    }
  };

  const currentVersionInfo = availableVersions.find(v => v.version === currentVersion);

  return (
    <div className="version-switcher">
      <button
        className="version-trigger"
        onClick={() => setIsOpen(!isOpen)}
        aria-label="Select version"
        aria-haspopup="listbox"
        aria-expanded={isOpen}
      >
        <span className="current-version">
          {currentVersionInfo?.label || currentVersion}
        </span>
        <span
          className="version-status"
          style={{ color: getStatusColor(currentVersionInfo?.status || '') }}
        >
          {getStatusLabel(currentVersionInfo?.status || '')}
        </span>
        <span className="dropdown-arrow">▼</span>
      </button>

      {isOpen && (
        <div className="version-dropdown" role="listbox">
          <div className="version-dropdown-header">
            <span>버전 선택</span>
          </div>

          {availableVersions.map((version) => (
            <button
              key={version.version}
              onClick={() => handleVersionSwitch(version)}
              className={`version-option ${
                version.version === currentVersion ? 'active' : ''
              }`}
              role="option"
              aria-selected={version.version === currentVersion}
            >
              <div className="version-info">
                <span className="version-label">{version.label}</span>
                <span
                  className="version-status-badge"
                  style={{ backgroundColor: getStatusColor(version.status) }}
                >
                  {getStatusLabel(version.status)}
                </span>
              </div>

              {version.isDefault && (
                <span className="default-badge">기본</span>
              )}

              {version.version === currentVersion && (
                <span className="current-indicator">✓</span>
              )}
            </button>
          ))}

          <div className="version-dropdown-footer">
            <a href="/releases" className="release-notes-link">
              릴리스 노트 보기
            </a>
          </div>
        </div>
      )}
    </div>
  );
}
```

### 버전 배너

버전 정보를 표시할 수 있습니다:

```typescript
// components/VersionBanner.tsx
import React from 'react';
import { useVersioning } from '@baidocs/versioning';

export function VersionBanner() {
  const { currentVersion, versionConfig } = useVersioning();
  const config = versionConfig[currentVersion];

  if (!config || config.status === 'active') {
    return null;
  }

  const bannerTypes = {
    maintenance: {
      className: 'version-banner-warning',
      icon: '⚠️',
      message: '이 버전은 유지보수 모드입니다.'
    },
    legacy: {
      className: 'version-banner-danger',
      icon: '⚠️',
      message: '이것은 레거시 버전입니다.'
    },
    preview: {
      className: 'version-banner-info',
      icon: '🧪',
      message: '이것은 미리보기 버전입니다.'
    }
  };

  const bannerInfo = bannerTypes[config.status] || bannerTypes.maintenance;

  return (
    <div className={`version-banner ${bannerInfo.className}`}>
      <div className="version-banner-content">
        <span className="version-banner-icon">{bannerInfo.icon}</span>
        <div className="version-banner-text">
          <strong>{bannerInfo.message}</strong>
          {config.endOfLife && (
            <span> 지원 종료일: {new Date(config.endOfLife).toLocaleDateString()}.</span>
          )}
          <a href="/latest" className="version-banner-link">
            최신 버전으로 전환
          </a>
        </div>
      </div>
    </div>
  );
}
```

## 자동화된 버전 관리

### 릴리스 자동화

버전 생성 및 관리를 자동화할 수 있습니다:

```yaml
# .github/workflows/release.yml
name: Create Documentation Release

on:
  push:
    tags:
      - 'v*'

jobs:
  create-docs-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Extract version from tag
        id: version
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          MAJOR_MINOR=$(echo $VERSION | sed 's/\.[0-9]*$//')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "major_minor=${MAJOR_MINOR}.x" >> $GITHUB_OUTPUT

      - name: Create version branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # 버전 브랜치 생성
          git checkout -b ${{ steps.version.outputs.major_minor }}

          # 새 버전에 대한 책 구성 업데이트
          sed -i 's/version: ".*"/version: "${{ steps.version.outputs.version }}"/' content/book.config.yaml

          # 버전 업데이트 커밋
          git add content/book.config.yaml
          git commit -m "Create documentation for ${{ steps.version.outputs.version }}"

          # 버전 브랜치 푸시
          git push origin ${{ steps.version.outputs.major_minor }}

      - name: Update main branch for next version
        run: |
          git checkout main

          # 다음 개발 버전으로 버전 업데이트
          NEXT_MINOR=$(($(echo ${{ steps.version.outputs.version }} | cut -d. -f2) + 1))
          NEXT_VERSION="$(echo ${{ steps.version.outputs.version }} | cut -d. -f1).$NEXT_MINOR.0-dev"

          sed -i "s/version: \".*\"/version: \"$NEXT_VERSION\"/" content/book.config.yaml

          git add content/book.config.yaml
          git commit -m "Prepare for next version: $NEXT_VERSION"
          git push origin main

      - name: Deploy versioned documentation
        run: |
          # 새 버전 배포 트리거
          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.DEPLOY_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{"version": "${{ steps.version.outputs.major_minor }}"}' \
            https://api.yoursite.com/deploy/docs
```

### 버전 정리

이전 버전을 자동으로 정리할 수 있습니다:

```yaml
# .github/workflows/cleanup-versions.yml
name: Cleanup Old Documentation Versions

on:
  schedule:
    - cron: '0 0 1 * *'  # 월간 정리

jobs:
  cleanup-old-versions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Identify old versions
        id: old-versions
        run: |
          # 모든 버전 브랜치 가져오기
          git fetch --all
          BRANCHES=$(git branch -r | grep "origin/v[0-9]" | sed 's/origin\///')

          # 2년 이상 된 버전 찾기
          OLD_BRANCHES=""
          for branch in $BRANCHES; do
            LAST_COMMIT_DATE=$(git log -1 --format=%ct origin/$branch)
            TWO_YEARS_AGO=$(($(date +%s) - 63072000))  # 2년 (초 단위)

            if [ $LAST_COMMIT_DATE -lt $TWO_YEARS_AGO ]; then
              OLD_BRANCHES="$OLD_BRANCHES $branch"
            fi
          done

          echo "old_branches=$OLD_BRANCHES" >> $GITHUB_OUTPUT

      - name: Archive old versions
        if: steps.old-versions.outputs.old_branches != ''
        run: |
          # 삭제 전 이전 버전의 아카이브 생성
          for branch in ${{ steps.old-versions.outputs.old_branches }}; do
            git checkout origin/$branch
            tar -czf "archive-$branch.tar.gz" content/
            # 아카이브 저장소에 업로드
          done

      - name: Delete old version branches
        if: steps.old-versions.outputs.old_branches != ''
        run: |
          for branch in ${{ steps.old-versions.outputs.old_branches }}; do
            git push origin --delete $branch
          done

      - name: Update version configuration
        run: |
          # 메인 브랜치 구성에서 이전 버전 제거
          # book.config.yaml을 업데이트하여 아카이브된 버전 제거
          git checkout main
          # ... 업데이트 로직 ...
          git add content/book.config.yaml
          git commit -m "Remove archived versions from configuration"
          git push origin main
```

## 버전 분석

### 버전 사용량 추적

사용자가 액세스하는 버전을 모니터링할 수 있습니다:

```typescript
// 버전 분석
interface VersionAnalytics {
  version: string;
  pageviews: number;
  uniqueUsers: number;
  avgSessionDuration: number;
  bounceRate: number;
  lastAccess: string;
}

async function getVersionAnalytics(timeframe: string = '30d'): Promise<VersionAnalytics[]> {
  const analytics = await analyticsAPI.getVersionStats({
    timeframe,
    metrics: ['pageviews', 'users', 'session_duration', 'bounce_rate']
  });

  return analytics.versions.map(version => ({
    version: version.name,
    pageviews: version.pageviews,
    uniqueUsers: version.unique_users,
    avgSessionDuration: version.avg_session_duration,
    bounceRate: version.bounce_rate,
    lastAccess: version.last_access
  }));
}

// 버전 사용 중단 알림
async function notifyVersionDeprecation() {
  const analytics = await getVersionAnalytics('90d');

  for (const version of analytics) {
    const versionConfig = await getVersionConfig(version.version);

    if (versionConfig.status === 'legacy' && version.pageviews > 1000) {
      // 여전히 많이 사용되는 레거시 버전 - 알림 전송
      await sendDeprecationNotification({
        version: version.version,
        usage: version.pageviews,
        endOfLife: versionConfig.endOfLife,
        migrationGuide: `/migrate-from-${version.version}`
      });
    }
  }
}
```

## 모범 사례

### 버전 전략

#### 시맨틱 버전 관리

시맨틱 버전 관리 원칙을 따르세요:

```yaml
# 버전 브랜치 명명 전략
versions:
  - branch: "main"        # 최신 개발
    version: "4.0.0-dev"

  - branch: "v3.x"        # 현재 안정 (3.x.x)
    version: "3.2.1"

  - branch: "v2.x"        # 이전 안정 (2.x.x)
    version: "2.8.5"

  - branch: "v1.x"        # 레거시 (1.x.x)
    version: "1.15.3"
```

#### 버전 수명 주기

버전 수명 주기를 관리하세요:

1. **개발** (`main`): 활발한 개발, 빈번한 변경
2. **릴리스 후보** (`rc/v3.0`): 기능 완성, 테스트
3. **안정** (`v3.x`): 릴리스된 버전, 버그 수정만
4. **유지보수** (`v2.x`): 보안 수정, 중요한 버그
5. **레거시** (`v1.x`): 지원 종료 일정 발표
6. **아카이브**: 더 이상 유지되지 않지만 문서 보존

### 콘텐츠 관리

#### 버전별 콘텐츠

```mdx
<!-- 좋음: 명확한 버전 표시 -->
:::info 버전 3.0+
이 기능은 버전 3.0 이상에서 사용할 수 있습니다.
:::

<!-- 좋음: 마이그레이션 가이드 -->
<VersionMigration from="2.x" to="3.x">
버전 2.x에서는 `oldMethod()`를 사용했습니다. 3.x에서는 `newMethod()`를 대신 사용하세요.
</VersionMigration>

<!-- 피해야 함: 불명확한 버전 관리 -->
이 새로운 기능을 이제 사용할 수 있습니다!
```

#### 일관된 네비게이션

가능할 때 버전 간에 일관된 네비게이션 구조를 유지하세요:

```yaml
# 모든 버전에서 사용되는 템플릿 네비게이션
navigationTemplate:
  - title: "시작하기"
    items:
      - title: "설치"
        path: "installation.mdx"
      - title: "빠른 시작"
        path: "quick-start.mdx"

  - title: "API 참조"
    items:
      # 버전별 API 문서
      - title: "인증"
        path: "api/auth.mdx"
      - title: "엔드포인트"
        path: "api/endpoints.mdx"
```

## 문제 해결

### 일반적인 문제

#### "Version Not Found"

```bash
# 버전 구성 확인
cat content/book.config.yaml | grep -A 20 "versioning:"

# 사용 가능한 브랜치 확인
git branch -r | grep "v[0-9]"

# 버전 브랜치 존재 확인
git ls-remote origin v3.x
```

#### "Version Switch Failed"

```typescript
// 버전 전환 디버그
const debugVersionSwitch = async (targetVersion: string) => {
  try {
    // 버전 존재 확인
    const available = await getAvailableVersions();
    if (!available.includes(targetVersion)) {
      throw new Error(`Version ${targetVersion} not available`);
    }

    // 버전에 대한 콘텐츠 존재 확인
    const content = await fetchVersionContent(targetVersion);
    if (!content) {
      throw new Error(`No content found for version ${targetVersion}`);
    }

    console.log(`Version ${targetVersion} is valid and has content`);
  } catch (error) {
    console.error('Version switch debug:', error);
  }
};
```

#### "Cross-Version Links Broken"

```typescript
// 버전 간 링크 수정
const fixCrossVersionLinks = (content: string, currentVersion: string) => {
  // 절대 버전 링크를 상대 링크로 변환
  return content.replace(
    /\/docs\/v[\d\.]+\/(.*?)\.md/g,
    (match, path) => `/docs/${currentVersion}/${path}.mdx`
  );
};
```

## 다음 단계

고급 버전 관리 주제:

1. **[다국어 지원](multilingual.mdx)** - 여러 언어에 대한 버전 관리
2. **[배포](../deployment/vercel.mdx)** - 여러 버전 배포
3. **[검색 설정](search.mdx)** - 버전 인식 검색

---

*효과적인 버전 관리는 사용자가 소프트웨어 버전에 적합한 문서에 액세스할 수 있도록 하면서 제품과 함께 문서를 발전시킬 수 있게 합니다.*