# Vercel 배포

전 세계적으로 확장 가능하고 유지보수가 필요 없는 문서 호스팅을 위해 BaiDocs를 Vercel에 배포하세요. 이 배포는 공개 액세스를 위한 뷰어 애플리케이션에 중점을 두고 에디터는 로컬 사용을 위해 유지합니다.

## 개요

### Vercel 배포 전략

BaiDocs는 개발과 프로덕션 모두에 최적화된 하이브리드 배포 모델을 따릅니다:

- **Vercel의 뷰어**: 전 세계적으로 액세스 가능한 공개 문서
- **로컬 에디터**: 콘텐츠 편집은 로컬 머신 또는 프라이빗 서버에서 유지
- **Git 통합**: 콘텐츠 변경 시 자동 배포
- **글로벌 CDN**: 전 세계 빠른 콘텐츠 전달

### 아키텍처

```
BaiDocs Vercel 배포:
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│ Local Editor    │    │ Git Repository  │    │ Vercel Viewer   │
│ (localhost:3001)│────│ (GitHub/GitLab) │────│ (Global CDN)    │
│                 │    │                 │    │                 │
│ - 콘텐츠 편집     │    │ - 소스 오브       │    │ - 공개 문서       │
│ - 실시간 미리보기  │    │   트루스         │    │ - 검색           │
│ - Git 푸시       │    │ - 자동 배포      │    │ - 빠른 로딩       │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

## 전제 조건

### 필수 계정

- **Vercel 계정**: [vercel.com](https://vercel.com)에서 회원가입
- **Git 저장소**: GitHub, GitLab 또는 Bitbucket 저장소
- **도메인 (선택사항)**: 브랜딩을 위한 사용자 정의 도메인

### 저장소 준비

저장소가 제대로 구조화되었는지 확인하세요:

```
your-docs-repository/
├── book.config.yaml          # BaiDocs 설정
├── en/                      # 영어 콘텐츠
│   ├── index.mdx
│   ├── getting-started/
│   └── _assets/
├── ko/                      # 한국어 콘텐츠 (선택사항)
│   ├── index.mdx
│   └── getting-started/
├── .gitignore              # 불필요한 파일 제외
└── README.md               # 저장소 문서
```

## Vercel 설정

### 1단계: 저장소 연결

1. **Vercel 로그인**:
   - [vercel.com](https://vercel.com)으로 이동
   - Git 제공업체로 로그인

2. **프로젝트 가져오기**:
   - "New Project" 클릭
   - 문서 저장소 선택
   - BaiDocs 콘텐츠가 포함된 저장소 선택

![Vercel Import Project](../_assets/vercel-import.png)

### 2단계: 빌드 설정 구성

빌드 및 배포 설정을 구성합니다:

**Framework Preset**: Custom

**빌드 설정**:
```bash
# Build Command
npm run build:viewer

# Output Directory
apps/viewer/.next

# Install Command
npm install

# Development Command
npm run dev:viewer
```

**환경 변수**:
```bash
# 필수 환경 변수
CONTENT_PATH=./content
NEXT_PUBLIC_VIEWER_URL=https://your-domain.vercel.app

# 선택적 커스터마이징
NODE_ENV=production
ENABLE_SEARCH=true
ENABLE_PDF_GENERATION=true
```

### 3단계: 프로젝트 설정

저장소 루트에 `vercel.json` 생성:

```json
{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npm run build:viewer",
  "outputDirectory": "apps/viewer/.next",
  "installCommand": "npm install",
  "functions": {
    "apps/viewer/src/app/api/**": {
      "memory": 1024
    }
  },
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    },
    {
      "source": "/_assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    },
    {
      "source": "/pdfs/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=604800"
        }
      ]
    }
  ],
  "redirects": [
    {
      "source": "/docs/:path*",
      "destination": "/:path*",
      "permanent": true
    }
  ],
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ],
  "regions": ["iad1", "sfo1", "lhr1", "hnd1"],
  "github": {
    "autoJobCancelation": false
  }
}
```

### 4단계: 배포

1. **프로젝트 배포**:
   - "Deploy" 클릭
   - 초기 배포가 완료될 때까지 대기
   - 제공된 URL에서 문서 확인

2. **배포 검증**:
   - 모든 페이지가 올바르게 로드되는지 확인
   - 내비게이션이 작동하는지 검증
   - 검색 기능 테스트
   - 이미지와 에셋이 로드되는지 확인

## 자동 배포

### Git 통합

변경사항을 푸시할 때 Vercel이 자동으로 배포합니다:

```bash
# 로컬에서 콘텐츠 변경
echo "# Updated Content" > en/new-page.mdx

# 변경사항 커밋 및 푸시
git add .
git commit -m "Add new documentation page"
git push origin main

# Vercel이 자동으로 푸시를 감지하고 배포
```

### 브랜치 배포

다양한 배포 전략 구성:

**프로덕션 배포** (main 브랜치):
```json
{
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
```

**프리뷰 배포** (기능 브랜치):
- 풀 리퀘스트에 대한 자동 프리뷰 URL
- main으로 병합하기 전 변경사항 테스트
- 리뷰용 공유 가능한 프리뷰 링크

### 배포 훅

외부 트리거를 위한 웹훅 설정:

```bash
# Vercel 대시보드에서 배포 훅 생성
# 훅 URL을 사용하여 배포 트리거

curl -X POST "https://api.vercel.com/v1/integrations/deploy/[HOOK_ID]" \
     -H "Content-Type: application/json" \
     -d '{
       "ref": "main",
       "target": "production"
     }'
```

## 사용자 정의 도메인 설정

### 1단계: 도메인 추가

1. **Vercel 대시보드에서**:
   - 프로젝트 설정으로 이동
   - "Domains" 클릭
   - 사용자 정의 도메인 추가

2. **도메인 유형**:
   - `docs.yourcompany.com` (서브도메인)
   - `yourcompany.com/docs` (경로 기반)
   - `yourdocs.com` (별도 도메인)

### 2단계: DNS 설정

DNS 레코드 구성:

**서브도메인용 (`docs.yourcompany.com`)**:
```
Type: CNAME
Name: docs
Value: cname.vercel-dns.com
```

**Apex 도메인용 (`yourdocs.com`)**:
```
Type: A
Name: @
Value: 76.76.19.19

Type: A
Name: @
Value: 76.76.21.21
```

### 3단계: SSL 인증서

Vercel이 자동으로 SSL 인증서를 제공합니다:
- 자동 SSL 프로비저닝
- 인증서 자동 갱신
- 기본적으로 HTTPS 리다이렉트 활성화

## 성능 최적화

### Edge Functions

Edge Functions로 성능 최적화:

```typescript
// middleware.ts
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export function middleware(request: NextRequest) {
  // 레거시 URL 리다이렉트
  if (request.nextUrl.pathname.startsWith('/old-docs')) {
    return NextResponse.redirect(
      new URL('/docs' + request.nextUrl.pathname.slice(9), request.url)
    );
  }

  // 보안 헤더 추가
  const response = NextResponse.next();

  response.headers.set('X-Frame-Options', 'DENY');
  response.headers.set('X-Content-Type-Options', 'nosniff');
  response.headers.set('Referrer-Policy', 'origin-when-cross-origin');

  return response;
}

export const config = {
  matcher: [
    /*
     * 다음으로 시작하는 것을 제외한 모든 요청 경로와 매치:
     * - api (API 라우트)
     * - _next/static (정적 파일)
     * - _next/image (이미지 최적화 파일)
     * - favicon.ico (파비콘 파일)
     */
    '/((?!api|_next/static|_next/image|favicon.ico).*)',
  ],
};
```

### 캐싱 전략

더 나은 성능을 위한 캐싱 최적화:

```json
{
  "headers": [
    {
      "source": "/_assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    },
    {
      "source": "/_next/static/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    },
    {
      "source": "/pdfs/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=604800"
        }
      ]
    },
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=3600, stale-while-revalidate=86400"
        }
      ]
    }
  ]
}
```

### 이미지 최적화

Vercel의 이미지 최적화 구성:

```typescript
// next.config.js
module.exports = {
  images: {
    domains: ['raw.githubusercontent.com', 'your-image-domain.com'],
    formats: ['image/webp', 'image/avif'],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
    minimumCacheTTL: 60,
  },
};
```

## 환경 설정

### 프로덕션 환경 변수

프로덕션별 설정 구성:

```bash
# Vercel Dashboard > Settings > Environment Variables에서

# 핵심 설정
CONTENT_PATH=./content
NODE_ENV=production

# URL
NEXT_PUBLIC_VIEWER_URL=https://docs.yourcompany.com
NEXT_PUBLIC_EDITOR_URL=http://localhost:3001

# 검색 설정
ENABLE_SEARCH=true
SEARCH_PROVIDER=vercel

# PDF 생성
ENABLE_PDF_GENERATION=true
PDF_QUALITY=high

# 분석 (선택사항)
VERCEL_ANALYTICS_ID=your-analytics-id
GOOGLE_ANALYTICS_ID=GA-XXXXXXXXX

# 콘텐츠 보안
CONTENT_SECURITY_POLICY=strict
```

### 다중 환경 설정

다양한 환경 구성:

**프로덕션** (main 브랜치):
```bash
ENVIRONMENT=production
NEXT_PUBLIC_API_URL=https://api.yourcompany.com
ENABLE_DEBUG=false
```

**스테이징** (develop 브랜치):
```bash
ENVIRONMENT=staging
NEXT_PUBLIC_API_URL=https://staging-api.yourcompany.com
ENABLE_DEBUG=true
```

**프리뷰** (기능 브랜치):
```bash
ENVIRONMENT=preview
NEXT_PUBLIC_API_URL=https://dev-api.yourcompany.com
ENABLE_DEBUG=true
```

## 모니터링 및 분석

### Vercel Analytics

Vercel의 내장 분석 활성화:

```typescript
// app/layout.tsx
import { Analytics } from '@vercel/analytics/react';

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        {children}
        <Analytics />
      </body>
    </html>
  );
}
```

### 사용자 정의 분석

Google Analytics 또는 다른 서비스 통합:

```typescript
// lib/analytics.ts
export const GA_TRACKING_ID = process.env.NEXT_PUBLIC_GA_ID;

export const pageview = (url: string) => {
  window.gtag('config', GA_TRACKING_ID, {
    page_path: url,
  });
};

export const event = ({ action, category, label, value }: {
  action: string;
  category: string;
  label?: string;
  value?: number;
}) => {
  window.gtag('event', action, {
    event_category: category,
    event_label: label,
    value: value,
  });
};
```

### 성능 모니터링

Core Web Vitals 모니터링:

```typescript
// lib/vitals.ts
import { getCLS, getFID, getFCP, getLCP, getTTFB } from 'web-vitals';

function sendToAnalytics(metric: any) {
  // 분석 서비스로 전송
  console.log(metric);
}

getCLS(sendToAnalytics);
getFID(sendToAnalytics);
getFCP(sendToAnalytics);
getLCP(sendToAnalytics);
getTTFB(sendToAnalytics);
```

## 보안 설정

### Content Security Policy

CSP 헤더 구현:

```json
{
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Content-Security-Policy",
          "value": "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://vercel.live https://www.googletagmanager.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self' https://vitals.vercel-insights.com; frame-ancestors 'none';"
        }
      ]
    }
  ]
}
```

### 액세스 제어

액세스 제한 구성:

```typescript
// middleware.ts - 민감한 문서에 인증 추가
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export function middleware(request: NextRequest) {
  // 내부 문서 보호
  if (request.nextUrl.pathname.startsWith('/internal')) {
    const authHeader = request.headers.get('authorization');

    if (!authHeader || !isValidToken(authHeader)) {
      return NextResponse.json(
        { error: 'Authentication required' },
        { status: 401 }
      );
    }
  }

  return NextResponse.next();
}

function isValidToken(token: string): boolean {
  // 인증 로직 구현
  return token === `Bearer ${process.env.ACCESS_TOKEN}`;
}
```

## CI/CD 통합

### GitHub Actions

GitHub Actions를 통한 자동화된 배포:

```yaml
# .github/workflows/deploy.yml
name: Deploy to Vercel

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

jobs:
  Deploy-Preview:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v3

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=preview --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build Project Artifacts
        run: vercel build --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy Project Artifacts to Vercel
        id: deploy
        run: |
          url=$(vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }})
          echo "preview_url=$url" >> $GITHUB_OUTPUT

      - name: Comment Preview URL
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `📖 Preview deployed: ${{ steps.deploy.outputs.preview_url }}`
            })

  Deploy-Production:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v3

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build Project Artifacts
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy Project Artifacts to Vercel
        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}

  Lighthouse:
    needs: Deploy-Preview
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v3

      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v9
        with:
          urls: |
            ${{ needs.Deploy-Preview.outputs.preview_url }}
          uploadArtifacts: true
          temporaryPublicStorage: true
```

## 문제 해결

### 일반적인 문제

#### 빌드 실패

```bash
# Vercel 대시보드에서 빌드 로그 확인
# 일반적인 문제와 해결책:

# 1. 종속성 누락
# 해결책: package.json에 필요한 모든 패키지가 포함되어 있는지 확인

# 2. 환경 변수 문제
# 해결책: Vercel 대시보드에서 필요한 모든 환경 변수가 설정되어 있는지 확인

# 3. 메모리 문제
# 해결책: vercel.json에서 함수 메모리 증가
{
  "functions": {
    "apps/viewer/src/app/api/**": {
      "memory": 1024
    }
  }
}
```

#### 배포 타임아웃

```json
{
  "builds": [
    {
      "src": "apps/viewer/package.json",
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "50mb"
      }
    }
  ]
}
```

#### 사용자 정의 도메인 문제

```bash
# DNS 설정 확인
dig docs.yourcompany.com

# CNAME 레코드 확인
nslookup docs.yourcompany.com

# Vercel 대시보드에서 SSL 인증서 상태 확인
```

### 성능 문제

#### 느린 빌드 시간

```json
{
  "build": {
    "env": {
      "SKIP_TYPE_CHECK": "true",
      "NEXT_TELEMETRY_DISABLED": "1"
    }
  }
}
```

#### 큰 번들 크기

```typescript
// next.config.js - 번들 분석
const withBundleAnalyzer = require('@next/bundle-analyzer')({
  enabled: process.env.ANALYZE === 'true',
});

module.exports = withBundleAnalyzer({
  // Next.js 설정
});
```

### 모니터링 및 디버깅

```bash
# 배포 상태 확인
vercel --version
vercel list
vercel logs [deployment-url]

# 빌드 문제 디버그
vercel build --debug

# 로컬에서 배포 테스트
vercel dev
```

## 모범 사례

### 콘텐츠 관리

1. **별도 콘텐츠 저장소**: 콘텐츠를 전용 저장소에 보관
2. **브랜치 전략**: 프로덕션용 main, 스테이징용 develop 사용
3. **커밋 메시지**: 명확하고 설명적인 커밋 메시지 사용
4. **리뷰 프로세스**: 콘텐츠 변경에 대해 풀 리퀘스트 사용

### 성능

1. **이미지 최적화**: Vercel의 이미지 최적화 사용
2. **정적 생성**: 가능할 때 페이지를 미리 생성
3. **캐싱**: 적절한 캐시 헤더 설정
4. **번들 최적화**: JavaScript 번들 크기 최소화

### 보안

1. **환경 변수**: Git에 비밀을 커밋하지 않음
2. **콘텐츠 보안**: CSP 헤더 구현
3. **액세스 제어**: 민감한 콘텐츠에 대한 액세스 제한
4. **정기 업데이트**: 종속성을 최신 상태로 유지

## 다음 단계

Vercel 배포를 완료하세요:

1. **[로컬 배포](local.mdx)** - 로컬 에디터 환경 설정
2. **[검색 설정](../advanced/search.mdx)** - 프로덕션용 검색 최적화
3. **[다국어 지원](../advanced/multilingual.mdx)** - 다국어 문서 배포

---

*Vercel 배포는 최소한의 유지보수와 최대 성능으로 문서를 위한 확장 가능한 글로벌 플랫폼을 제공합니다.*